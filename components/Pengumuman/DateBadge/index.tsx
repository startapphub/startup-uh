import { type FC } from 'react';

const DateBadge: FC = () => (
  <span className="text-base h-fit w-fit font-bold text-gray-100 box-light-gray px-3 rounded-full">
    10 April
  </span>
);

export default DateBadge;
